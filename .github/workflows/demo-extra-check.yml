name: "Demo Extra Check"

on:
  pull_request:
    branches: [develop, main]
    types: [opened, edited, reopened, synchronize, ready_for_review]

permissions:
  contents: read

jobs:
  demo-extra-check:
    runs-on: ubuntu-latest
    steps:
      - name: Demo check
        run: |
          echo "Demo extra workflow running alongside shared checks."
